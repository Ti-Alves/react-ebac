import React from "react";
import { BigText, HeaderS } from "../Style/Style";

export default function Header() {
    return (
        <HeaderS>
            <BigText>Meus Livros</BigText>
        </HeaderS>
    );
}